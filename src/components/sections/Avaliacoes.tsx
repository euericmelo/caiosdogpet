// TODO: Ativar esta seção quando existirem avaliações reais
export function Avaliacoes() {
  return null;

  /* Estrutura pronta para o futuro:
  return (
    <section id="avaliacoes" className="py-24 bg-background relative overflow-hidden">
      <div className="container mx-auto px-4">
        <h2 className="text-4xl md:text-5xl font-black text-center mb-16 tracking-tight">
          O que dizem os tutores
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
          {reviews.map((r, i) => (
             <Card key={i} className="border-none shadow-lg p-8 space-y-6 rounded-[32px] bg-secondary/20">
                // Avaliação content here
             </Card>
          ))}
        </div>
      </div>
    </section>
  );
  */
}
